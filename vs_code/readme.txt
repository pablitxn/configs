"setting.json" es el archivo correspondiente a la configuración global de vs-code, su directorio es:

    .config > Code > User > ...
    
"keybindings.json" hace referencia a la configuración de atajos del teclado.
También va en el mismo directorio de "setting.json":

    .config > Code > User > ...
